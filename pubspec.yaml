name: iiportfo
description: Another portfo management app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.1
  intl: ^0.17.0
  file_picker: ^3.0.1
  shared_preferences: ^2.0.5
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - lib/assets/secrets.json
    - lib/assets/img/irr-logo.png